repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: .venv/bin/python -m pytest tests/ -v --tb=short
        language: system
        pass_filenames: false
        always_run: true
